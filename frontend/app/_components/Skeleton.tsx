// instead of import Skeleton and css file in every page we combine them together here and export only Skeleton as default for refactoring imports

import Skeleton from "react-loading-skeleton"
import "react-loading-skeleton/dist/skeleton.css"

export default Skeleton
